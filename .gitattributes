# Ensure all text files use CRLF in the repository
* text=auto eol=crlf

# Custom settings for specific file types
*.txt text eol=crlf
*.sh text eol=lf   # Shell scripts often need LF for compatibility with Unix/Linux environments
*.bat text eol=crlf

# Handle whitespace for diff and merging
*.cpp diff=cpp
*.java whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol